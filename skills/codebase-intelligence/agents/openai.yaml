interface:
  display_name: "Codebase Intelligence"
  short_description: "Route cross-file symbol work through Serena"
  default_prompt: "Use $codebase-intelligence to trace this symbol and its references."
dependencies:
  tools:
    - type: "mcp"
      value: "codebase"
      description: "Global Serena semantic code index"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
